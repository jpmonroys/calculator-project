image: python:3.11

pipelines:
  default:
    - step:
        name: Install Dependencies
        script:
          - pip install -r requirements.txt

    - step:
        name: Code Quality
        script:
          - flake8 .

    - step:
        name: Run Tests
        script:
          - pytest
